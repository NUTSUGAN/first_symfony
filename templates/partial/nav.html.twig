<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container px-5">
        <a class="navbar-brand" href="{{ path('app_home') }}">IPSSI</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_category') }}">Catégories</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_post_index') }}">Posts</a>
                </li>
            </ul>

            <div class="d-flex gap-2">
                <a class="btn btn-outline-light btn-sm" href="{{ path('app_category_new') }}">+ Catégorie</a>
                <a class="btn btn-warning btn-sm" href="{{ path('app_post_new') }}">+ Post</a>
            </div>
        </div>
    </div>
</nav>
